version: '2.1'  
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true
    resource_class: vandit/lease
    steps:
      - checkout
      - matlab/run-tests:
          use-parallel: true  


          
workflows:
  number_parpool_self_8:
    jobs: 
      - build
