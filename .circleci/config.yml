version: '2.1' 
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true 
    resource_class: vandit_lease 
    parallelism: 2  
    steps:
      - run:  
          name: Run MATLAB test with test splitting
          command: |
              cd unittest/
              ls
              

      - store_test_results:
          path: tests/results.xml              


          
workflows:
  circleci_test_split_actual_test:
    jobs:
      - build

 