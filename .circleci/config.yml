version: '2.1'
orbs:
  matlab: mathworks/matlab@1.0.0
  
jobs:
     
  build_script:
    machine:
      image: ubuntu-2204:2022.07.1
      
    steps:
      - checkout
      - matlab/install
      - matlab/run-command:
          command: myscript
          
          
          
workflows:
  build:
    jobs:
      - build_script
