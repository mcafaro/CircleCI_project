version: '2.1'
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine:
      image: ubuntu-2004:current
    parallelism: 2  
    steps:
      - checkout
      - run: 
          name: display glob
          command: |
              cd tests/
              echo $(circleci tests glob "**/*.m" | xargs -n1 basename | sed 's/\.m$//')
                   
workflows:
  glob_learning:
    jobs:
      - build