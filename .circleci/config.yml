version: '2.1'
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true
    resource_class: vandit/class_window 
    steps:
      - checkout
      - run:
          command: |
            pwd
            ls
workflows:
  self_hosted:
    jobs:
      - build