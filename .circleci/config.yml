version: '2.1' 
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true
    resource_class: vandit/class_window  
    steps: 
      - checkout    
      - matlab/run-command:
          command: pwd
                           
workflows:
  self_hosted_runner_run_test_use_parallel:
    jobs:
      - build