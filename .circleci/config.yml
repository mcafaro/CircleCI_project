version: '2.1'
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true
    resource_class: vandit/r_s
    steps: 
      - checkout   
      - matlab/run-tests
                           
workflows:
  self_hosted_runner_matlab_run_tests:
    jobs:
      - build     