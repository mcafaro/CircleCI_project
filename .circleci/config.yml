version: '2.1'
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true
    resource_class: vandit/class_window 
    steps:
      - matlab/install
      - matlab/run-tests


 
            
                   
workflows:
  self_hosted_runner_network_repo:
    jobs:
      - build