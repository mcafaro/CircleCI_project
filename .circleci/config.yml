version: '2.1'
orbs:
<<<<<<< HEAD
  matlab: mathworks/matlab@
=======
  matlab: mathworks/matlab@1

>>>>>>> f479d9a312a6c615a83495c3d03c86b5b91ff23a
jobs:
  build:
    machine:
      image: ubuntu-2004:current
    parallelism: 2 

    steps:
      - checkout
      - matlab/install
      - run: 
          name: Run MATLAB test with test splitting
          command: |
              TESTFILES=$(circleci tests glob "tests/*.m" | circleci tests split)  
              echo "$TESTFILES"
              FORMATTED_TESTFILES="'$(echo $TESTFILES | sed "s/ /', '/g")'"
              matlab-batch "addpath('tests'); runTestsAndGenerateReports" 
          
      - store_test_results:
          path: test-results
workflows:
  add_env:
    jobs:
      - build