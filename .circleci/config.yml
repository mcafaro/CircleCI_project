version: '2.1' 
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true
    resource_class: vandit/lease
    steps: 
      - run: 
          command: pwd
          
      - matlab/run-tests:
          select-by-folder: \unittest
                         
workflows:  
  actual_test_cases_2:
    jobs:  
      - build  
