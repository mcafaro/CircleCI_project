version: 2.1                   
orbs: 
  matlab: mathworks/matlab@1  
jobs:   
  build:
    machine: true  
    resource_class: vandit/lease 
    parallelism: 2  
    steps: 
      - run:
          command: |
              matlab -batch "my_genscript"
 
      - store_test_results: 
          path: results.xml

      - store_artifacts: 
          path: results.xml
          destination: results.xml

      - store_artifacts:
          path: results.pdf
          destination: results.pdf

workflows:   
  sim_test_pdg_generation: 
    jobs:
      - build             
             

 