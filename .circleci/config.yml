version: '2.1'
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true 
    resource_class: vandit/lease
    steps:
      - matlab/run-tests:
          select-by-folder: \tests
          use-parallel: true
                   
workflows:
  self_parpool_4:
    jobs:
      - build 