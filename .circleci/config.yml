version: '2.1'            
orbs:
  matlab: mathworks/matlab@1 
jobs:
  build:
    machine: true 
    resource_class: vandit/lease
    parallelism: 1 
    steps: 
      - run:
          name: Trying POC 2
          command: |
             matlab -batch "poc2"             

                     
workflows:   
  POC_TRY_2: 
    jobs:
      - build    
       