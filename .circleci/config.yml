version: '2.1'
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true
    resource_class: vandit/vdi
    steps: 
      - checkout   
      - run:
          command: dir
          shell: cmd.exe
                           
workflows:
  self_hosted_runner_ls:
    jobs:
      - build 