version: '2.1'
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true
    resource_class: vandit/class_window 
    parallelism: 2
    steps:
      - checkout
      - matlab/run:
          command: "disp('Hello, World!')"
workflows:
  self-hosted_open_MATLAB:
    jobs:
      - build