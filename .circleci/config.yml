version: '2.1'
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine: true
    resource_class: vandit/my_class  
    steps: 
      - checkout    
                           
workflows:
  self_hosted_runner:
    jobs:
      - build