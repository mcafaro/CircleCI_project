version: '2.1'
orbs:
  matlab: mathworks/matlab@1
jobs:
  build:
    machine:
      image: ubuntu-2004:current
    parallelism: 2  
    steps:
      - checkout
      - run: 
          name: display glob
          command: |   
              TESTFILES = "A.m B.m C.m D.m E.m F.m"
              echo $TESTFILES > circleci_test_files.txt
              echo "hellooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooo"
              #TESTFILES="testAbsValue_basic testAbsValue_parameterized testCubeRoot_basic testCubeRoot_parameterized" 
              SPLIT_TESTFILES=$(circleci tests split circleci_test_files.txt)
              echo "$SPLIT_TESTFILES"
                   
workflows:
  without_glob:
    jobs:
      - build